# Core HTTP & WebSocket stack (pure-Python or has 3.10 wheels)
aiohttp==3.9.5
aiosignal==1.3.1
frozenlist==1.4.1
multidict==6.0.5
yarl==1.9.4

# Utilities
python-dotenv==1.0.1
aioconsole==0.6.2
aiofiles==23.2.1
requests==2.31.0
ujson==5.10.0

# Optional speed-up: only install if a wheel is available, skip source build
# uvloop is Linux-only and requires a C compiler; pin to a version with py310 wheels
uvloop==0.19.0 ; sys_platform == "linux"

